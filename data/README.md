# Local Data Storage
This directory is intentionally left empty in the repository. When you run the agent locally, the SQLite database (`waymo_metrics.db`) will be automatically generated and stored here.

Database files are ignored by version control to prevent merge conflicts and keep the repository pristine.