# Pipeline Exports
This directory serves as the output destination for the Waymo Perception Agent. 

* `csv/` will contain the flattened `raw_perception_data.csv` database extracts.
* `graphs/` will contain native `.svg` visualizations generated by the dashboard module.

All generated data files in this directory are intentionally ignored by version control to protect data privacy and prevent repository bloat.